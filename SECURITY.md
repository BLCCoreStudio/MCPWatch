# Security Policy

MCPWatch is under active development. Local configuration snapshots may contain sensitive endpoint or command metadata, so future persistence must avoid unnecessary secret storage and must remain local by default.

Report vulnerabilities privately through GitHub private vulnerability reporting when available or another appropriate private channel. Never attach real access tokens, API keys, or private configuration secrets.
